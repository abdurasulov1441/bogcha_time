import 'package:flutter/material.dart';

class Activates extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("📚 Mashg‘ulotlar sahifasi"));
  }
}