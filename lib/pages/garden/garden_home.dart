import 'package:flutter/material.dart';

class GardenHome extends StatelessWidget {
  const GardenHome({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Bog\'cha vaqtini boshqarish')),
      body: const Center(child: Text('Bog\'cha vaqtini boshqarish')),
    );
  }
}
